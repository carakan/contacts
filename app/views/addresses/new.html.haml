- title "New Address"
- form_for @address, :url => b_card_addresses_path(params[:b_card_id]) do |f|
  = render :partial => 'form', :locals => {:f => f}

%p= link_to "Back to List", b_card_addresses_path(params[:b_card_id])
