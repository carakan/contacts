- title "New Phone"
- form_for @phone, :url => b_card_phones_path(params[:b_card_id]) do |f|
  = render :partial => 'form', :locals => {:f => f}

%p= link_to "Back to List", b_card_phones_path(params[:b_card_id])
