- title "Edit B Card"

- form_for @address, :url => b_card_address_path(params[:b_card_id], @address) do |f|
  = render :partial => 'form'

%p
  = link_to "Show", b_card_path(@b_card)
  |
  = link_to "View All", b_cards_path
